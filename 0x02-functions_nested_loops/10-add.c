#include "main.h"
/**
 * add - adds two numbers and returns the result
 *
 * @a: first parametr
 * @b: second parametr
 *
 * Return: int
 */
int add(int a, int b)
{
	int sum;

	sum = (a + b);
	return (sum);
}
