#include "main.h"
/**
 * _islower - cheaks for lowercase charachter
 *@c: parameter of the function
 * Return: int
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
}
}
