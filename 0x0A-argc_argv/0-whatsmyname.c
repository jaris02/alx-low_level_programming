#include <stdio.h>
/**
 * main - Prints the nmae of the program
 * @argc: The number of argument
 * @argv: An array of arguments.
 * Return: Always o.
 */
int main(int __attribute__((__unused__)) argc, char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
