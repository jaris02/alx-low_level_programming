#include "main.h"
/**
 * mul - a function to return multplication of two numbers
 * @a: first parameter
 * @b: second parameter
 * Return: int
 */
int mul(int a, int b)
{
	int z;

	z = a * b;
	return (z);
}
